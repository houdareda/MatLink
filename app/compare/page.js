import CompareClient from "./CompareClient";

export const metadata = {
  title: "مقارنة المنتجات | مات لينك",
  description: "قارن بين أفضل مواد البناء جنباً إلى جنب",
};

export default function ComparePage() {
  return <CompareClient />;
}
